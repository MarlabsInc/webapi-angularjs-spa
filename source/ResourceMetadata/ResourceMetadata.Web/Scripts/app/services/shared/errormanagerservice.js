app.factory('errorMngrSvc', function () {
    return {
        handleError: function (reason) {
            //if (typeof reason === 'object') {
            //    if (reason.Message) {
            //        alert(reason.Message);
            //    }
            //}
            //if (typeof reason === 'string') {
            //    alert(reason);
            //}
        }
    };
});